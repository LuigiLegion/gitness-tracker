// Initial State
const initialState = {};

// Action Types

// Action Creators

// Thunk Creators

// Reducer
const leaderboardReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default leaderboardReducer;
